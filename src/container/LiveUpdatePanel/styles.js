import styled from "styled-components";
import { Div } from "../../style-system/components";

const LiveUpdateContainer = styled(Div).attrs({
  space: { p: 4 },
})`
  max-width: 1400px;
  margin: auto;
`;

export default LiveUpdateContainer;
